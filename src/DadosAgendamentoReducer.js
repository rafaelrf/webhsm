//import moment from 'moment';

const INITIAL_STATE = {
    solicitacoesexames: []
}

export default (state = INITIAL_STATE, action) => {
    switch (action.type) {
        default:
            return state
    }
}